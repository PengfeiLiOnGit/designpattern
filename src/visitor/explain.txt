访问者模式：
表示一个作用域某对象结构中的各元素的操作。
它使你可以在不改变各元素的类的前提下定义作用与这些元素的新操作。

前提：适用于数据接口相对稳定的系统。
数据结构和作用域结构上的操作之间的耦合解脱开，使得操作集合可以相对自有的演化。

目的：把处理（方法）从数据结构（实体类）中分离出来。

优点：增加新的操作很容易（行为，方法，分支状态），因为增加新的操作意味着增加一个新的访问者。
访问者模式将有关的行为集中到一个访问中对象中。

