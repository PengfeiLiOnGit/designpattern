享元模式：
运用共享技术有效的支持大量细粒度的对象。

应用：如果一个应用使用了大量的对象，而大量的这些对象造成了很大的存储开销时就应该考虑使用；还有就是对象的大多数状态可以是外部状态，
如果删除对象的外部状态，那么可以用相对较少的共享对象取代很多组对象，此时可以考虑使用享元模式。

个人理解，享元模式是单例的模式应用，是组合，是体现。

内部状态主要与共享及复用、通用信息有关，同时可以组合外部状态，利用外部状态进行相应的改变。